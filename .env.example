# Jules CodeRabbit environment configuration
# Copy this file to `.env` (or configure equivalent runtime variables)
# and fill in values appropriate for your deployment.

# Base URL where this service is reachable (must be HTTPS in production).
SERVICE_BASE_URL=https://your-public-service-url.example.com

# Toggle whether the generated manifest is publicly visible.
# Accepts "true" or "false". Defaults to false when unset.
MANIFEST_PUBLIC=false

# Override the GitHub API base URL if using GitHub Enterprise.
# Typically you can keep the default value below.
GITHUB_API_BASE_URL=https://api.github.com
